import ReactLoading from 'react-loading'

const Loading = () => (
  <div className="flex justify-center">
    <ReactLoading type="spin" color="rgb(148 163 184)" width={30} />
  </div>
)

export default Loading
